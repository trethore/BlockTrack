generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model WatchlistItem {
  id        Int     @id @default(autoincrement())
  symbol    String
  isFavorite Boolean @default(false)
}
